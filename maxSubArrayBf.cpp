#include<iostream>
using namespace std;

int sum, len = nums.size(), maxi;
for(int i = 0; i < len;i++)
{
  sum = 0;
  for(int j = 0; j < len; j++)
  {
    sum =+nums[i];
    maxi = max(sum, maxi);
  }
}  
